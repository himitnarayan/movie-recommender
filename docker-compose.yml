version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
